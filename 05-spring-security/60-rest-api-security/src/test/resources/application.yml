spring:
    datasource:
        url: jdbc:h2:mem:testdb
    sql:
        init:
            mode:
                embedded
    h2:
        console:
            enabled: true
    jpa:
        properties:
            hibernate:
                format_sql: true
        show-sql: true
